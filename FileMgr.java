import java.util.*;
import java.io.*;
import Mgr.java;

public class FileMgr {
    static Scanner s = new Scanner(System.in);
    static File file = new File("test.txt");
    // static String[] names = new String[50];// 名字
    // static String[] codes = new String[50];// 密码

    // static String[][] cards = new String[100][2];//
    // 第一列存卡号，第二列存是谁的，与name对应的数组元素号对应
    // static double[] moneys = new double[100];// 钱数与card对应
    // static int[] types = new int[100];// 1普通卡2信用卡

    // public static void main(String[] args) {

    // /*
    // * try{//读东西并打印 FileReader reader=new FileReader(file); BufferedReader
    // * breader=new BufferedReader(reader); String record=new String(); while
    // * ((record=breader.readLine())!=null) { if(record.indexOf("name:")==0)
    // * record=deleteString(record,5); if(record.indexOf("code:")==0)
    // * record=deleteString(record,5); if(record.indexOf("money:")==0)
    // * record=deleteString(record,6); if(record.indexOf("card:")==0)
    // * record=deleteString(record,5); System.out.println(record); } }
    // * catch(IOException e){}
    // */
    // addUser();
    // addCard();
    // toArray();
    // showInfo();
    // }

}
